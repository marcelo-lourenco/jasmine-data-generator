'use strict';

var dbGeneral = require('../data/db-general.cjs');
var utils = require('../utils.cjs');

/**
 * Uma categoria de CNH aleatória.
 * @type {string}
 * @returns {string} - Uma escolaridade gerada aleatoriamente.
 * @example
 * console.log(cnhCategoria()); //Exemplo: "A", "B", "C", etc.
 */
function cnhCategoria() {
  return utils.getRandom(dbGeneral.categoriasCnh);
}

exports.cnhCategoria = cnhCategoria;
